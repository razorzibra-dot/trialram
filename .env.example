# ============================================================================
# MULTI-BACKEND API CONFIGURATION - PHASE 4
# ============================================================================
# 
# PHASE 4: UNIFIED MULTI-BACKEND SUPPORT
# This application now supports THREE backend modes:
# 1. Mock/Static Data API - Uses local static data (for development/testing)
# 2. Real .NET Core Backend API - Connects to actual backend server
# 3. Supabase PostgreSQL - Real-time database with RLS (Phase 3)
#
# RECOMMENDED APPROACH:
# Use VITE_API_MODE (new) instead of VITE_USE_MOCK_API (legacy)
# VITE_API_MODE takes precedence if both are set
#
# TO SWITCH BACKENDS:
# Option 1 - Global mode (all services):
#   VITE_API_MODE=mock|real|supabase
#   
# Option 2 - Per-service override (example):
#   VITE_API_MODE=real
#   VITE_CUSTOMER_BACKEND=supabase  # Override: use Supabase for customers
#   VITE_SALES_BACKEND=supabase     # Override: use Supabase for sales
#
# Restart development server after changing
# For detailed guide, see: docs/API_SWITCHING_GUIDE.md
# ============================================================================

# API Mode Selection (NEW - Phase 4)
# Options: mock | real | supabase
# mock     = Static/demo data (development/testing)
# real     = .NET Core backend (production API)
# supabase = PostgreSQL with real-time (modern production)
VITE_API_MODE=supabase

# Backward Compatibility (Legacy)
# Still supported but VITE_API_MODE takes precedence
# Set to 'true' for mock, 'false' for real API
VITE_USE_MOCK_API=false

# API Environment (development, staging, production)
# This determines which backend configuration to use
VITE_API_ENVIRONMENT=development

# Real API Configuration
# Base URL for the .NET Core backend API (only used when VITE_USE_MOCK_API=false)
# Development: http://localhost:5137/api/v1
# Staging: https://api-staging.yourcompany.com/api/v1
# Production: https://api.yourcompany.com/api/v1
VITE_API_BASE_URL=http://localhost:5137/api/v1

# API request timeout in milliseconds
VITE_API_TIMEOUT=30000

# ============================================================================
# PER-SERVICE BACKEND OVERRIDES (Phase 4 - Optional)
# ============================================================================
# Leave commented out to use global VITE_API_MODE
# Uncomment to override specific services: mock | real | supabase
# Example: Use Supabase for customers but real API for everything else
# VITE_CUSTOMER_BACKEND=supabase
# VITE_SALES_BACKEND=supabase
# VITE_TICKET_BACKEND=supabase
# VITE_CONTRACT_BACKEND=supabase
# VITE_USER_BACKEND=mock
# VITE_DASHBOARD_BACKEND=real
# VITE_NOTIFICATION_BACKEND=supabase
# VITE_FILE_BACKEND=real
# VITE_AUDIT_BACKEND=real

# Authentication
VITE_AUTH_TOKEN_KEY=crm_auth_token
VITE_AUTH_REFRESH_TOKEN_KEY=crm_refresh_token
VITE_AUTH_USER_KEY=crm_user

# Features
VITE_ENABLE_LOGGING=true
VITE_ENABLE_METRICS=true
VITE_ENABLE_OFFLINE=false

# File Upload
VITE_MAX_FILE_SIZE=10485760
VITE_ALLOWED_FILE_TYPES=.pdf,.doc,.docx,.xls,.xlsx,.jpg,.jpeg,.png,.gif

# Notification
VITE_ENABLE_PUSH_NOTIFICATIONS=true
VITE_VAPID_PUBLIC_KEY=

# Analytics
VITE_ENABLE_ANALYTICS=true
VITE_ANALYTICS_ENDPOINT=

# Security
VITE_ENABLE_CSP=true
VITE_ENABLE_HTTPS_ONLY=false

# Development
VITE_DEBUG_MODE=true
VITE_SHOW_API_LOGS=true

# ============================================================================
# QUICK REFERENCE
# ============================================================================
# 
# Development with Mock Data:
#   VITE_USE_MOCK_API=true
#   VITE_API_ENVIRONMENT=development
#
# Development with Real Backend:
#   VITE_USE_MOCK_API=false
#   VITE_API_ENVIRONMENT=development
#   VITE_API_BASE_URL=http://localhost:5137/api/v1
#   (Ensure backend is running: cd backend && dotnet run)
#
# Staging:
#   VITE_USE_MOCK_API=false
#   VITE_API_ENVIRONMENT=staging
#   VITE_API_BASE_URL=https://api-staging.yourcompany.com/api/v1
#
# Production:
#   VITE_USE_MOCK_API=false
#   VITE_API_ENVIRONMENT=production
#   VITE_API_BASE_URL=https://api.yourcompany.com/api/v1
#
# ============================================================================
