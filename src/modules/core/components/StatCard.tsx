/**
 * StatCard Component - Re-export
 * Re-exports the StatCard component from the common components directory
 * This maintains the modular architecture while providing a consistent import path
 */

export { StatCard, type StatCardProps } from '@/components/common/StatCard';