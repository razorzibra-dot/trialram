/**
 * Configuration Test Page - Modular Version
 */
import React from 'react';

// Re-export the existing component for now, will refactor content later if needed
import ConfigurationTest from '@/pages_backup/ConfigurationTest';

export const ConfigurationTestPage: React.FC = () => {
  return <ConfigurationTest />;
};

export default ConfigurationTestPage;