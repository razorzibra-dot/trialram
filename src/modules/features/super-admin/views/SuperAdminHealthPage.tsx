/**
 * Super Admin Health Page - Modular Version
 */
import React from 'react';

// Re-export the existing component for now, will refactor content later if needed
import SuperAdminHealth from '@/pages_backup/SuperAdminHealth';

export const SuperAdminHealthPage: React.FC = () => {
  return <SuperAdminHealth />;
};

export default SuperAdminHealthPage;