/**
 * Super Admin Role Requests Page - Modular Version
 */
import React from 'react';

// Re-export the existing component for now, will refactor content later if needed
import SuperAdminRoleRequests from '@/pages_backup/SuperAdminRoleRequests';

export const SuperAdminRoleRequestsPage: React.FC = () => {
  return <SuperAdminRoleRequests />;
};

export default SuperAdminRoleRequestsPage;