/**
 * Ticket Attachment Service (Supabase Implementation)
 * Re-export from the root supabase folder for consistent import paths
 */

export { ticketAttachmentService } from '../../supabase/ticketAttachmentService';
export type { CreateAttachmentData, TicketAttachmentFilters } from '../../supabase/ticketAttachmentService';
