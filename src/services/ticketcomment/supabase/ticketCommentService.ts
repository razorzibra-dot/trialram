/**
 * Ticket Comment Service (Supabase Implementation)
 * Re-export from the root supabase folder for consistent import paths
 */

export { ticketCommentService } from '../../supabase/ticketCommentService';
export type { CreateCommentData, TicketCommentFilters } from '../../supabase/ticketCommentService';
